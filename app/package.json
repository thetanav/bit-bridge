{
  "name": "app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prebuild": "echo 'No frontend prebuild step'",
    "build": "echo 'Frontend build done'",
    "start": "echo 'Starting frontend...'",
    "test": "echo 'No frontend tests'"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  }
}
